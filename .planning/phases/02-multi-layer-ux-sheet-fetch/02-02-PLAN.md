---
phase: 02-multi-layer-ux-sheet-fetch
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app.js
autonomous: false
requirements: [R2.1, R2.2, R2.3, R2.4, R2.5, R3.1]
must_haves:
  truths:
    - "工單 URL 格式錯誤時 showNotify 顯示『工單 URL 格式錯誤，請確認是 Google Sheet 連結』"
    - "Sheet 未公開（fetch 失敗 / 非 2xx）時 showNotify 顯示『無法讀取 Sheet，請確認已設為「知道連結的任何人可檢視」』"
    - "CSV 格式錯（parse throw 或空陣列）時 showNotify 顯示『Sheet 欄位格式錯誤，請確認第一列為標題、從第二列開始填』"
    - "管理頁「🔄 重新拉工單」：本地已有工單時，比對新舊差異後彈 showConfirm（含新增/刪除/變更清單），使用者同意才覆蓋並推 saveWorkOrder"
    - "新舊工單完全相同時不彈 confirm，直接 showNotify('工單無變更','ok')"
    - "本地無工單快取（首次拉）時不彈差異 confirm，直接寫入 + saveWorkOrder"
    - "拉取失敗時既有本地快取仍可用（fallback 不變）"
  artifacts:
    - path: "app.js"
      provides: "fetchTodayWorkOrder 錯誤分三類 throw；refreshWorkOrder 增加 diff + showConfirm；saveSheetUrl 共用錯誤分類訊息"
      contains: "WorkOrderError|SHEET_ERROR"
  key_links:
    - from: "app.js refreshWorkOrder()"
      to: "showConfirm() (02-01 提供)"
      via: "await showConfirm('重新拉工單', diffHtml, '覆蓋', '取消') → false 則 return"
      pattern: "showConfirm\\([^)]*覆蓋"
    - from: "app.js fetchTodayWorkOrder catch paths"
      to: "showNotify()"
      via: "error code → 對應三種訊息"
      pattern: "showNotify\\([^)]*Sheet"
    - from: "app.js refreshWorkOrder success path"
      to: "saveWorkOrder (Apps Script)"
      via: "Storage.setWorkOrder(newWo) 後呼叫既有 syncWorkOrderToCloud（或等效推送函式）"
---

<objective>
落實 D2（Sheet 拉取三類錯誤顯示 showNotify 橫幅）與 D3（重新拉工單 diff + 確認 modal 覆蓋）。Sheet URL 綁定、CSV 拉取、1hr 快取、saveWorkOrder 自動推送都已在 commit e3bb1e3 就位；本 plan 只補錯誤分類與差異確認 UX。

Purpose: 讓班長換 gid 時能清楚看到工單有哪些變動；Sheet 拉取失敗時給操作員明確可行動的錯誤訊息。
Output: app.js 內 fetchTodayWorkOrder / saveSheetUrl / refreshWorkOrder 錯誤處理細化 + diff 確認流程。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-layer-ux-sheet-fetch/02-CONTEXT.md
@.planning/phases/02-multi-layer-ux-sheet-fetch/02-01-SUMMARY.md
@SDD.md
@app.js

<interfaces>
現有程式（app.js）關鍵函式（已實作，只需改造不重寫）：

- `sheetUrlToCsv(url)` 第 ~735 行：URL 無效時回傳 null
- `parseWorkOrderCsv(csv)` 第 766 行：`lines.length < 2` 或全列無效 batch → throw 'Sheet 沒有資料'；可能回傳空陣列 layers
- `fetchTodayWorkOrder(force)` 第 788 行：目前只 throw 粗略訊息；需改為拋出含錯誤碼的 Error
- `saveSheetUrl()` 第 808 行、`refreshWorkOrder()` 第 826 行：目前 catch 只把 err.message 塗到 sheetStatus；需改為依錯誤碼叫對應 showNotify
- `Storage.getWorkOrderRaw()` / `Storage.setWorkOrder(wo)`：讀寫本地工單快取
- `syncWorkOrderToCloud` 或等效 `saveWorkOrder` 推送（e3bb1e3 已加）— grep 找確切函式名
- 02-01 交付的 `showConfirm(title, bodyHtml, okLabel, cancelLabel)` → Promise<boolean>
- `showNotify(text, level)`：level 用 'error'（紅色 3-5 秒）/'ok'/'success'/'warn'

WorkOrder layer 結構：`{ batch, batchNum, layer, product, flavor, notes }`，key 為 `batch`（如 "1-1"）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchTodayWorkOrder 錯誤分類 + saveSheetUrl/refreshWorkOrder 對應 showNotify</name>
  <files>app.js</files>
  <behavior>
    - Case A（URL 格式錯）: `sheetUrlToCsv` 回 null → throw Error with `err.code === 'BAD_URL'`
    - Case B（Sheet 未公開 / fetch 失敗）: fetch reject 或 `!resp.ok` → throw with `err.code === 'NOT_PUBLIC'`
    - Case C（CSV 格式錯）: parseWorkOrderCsv throw 或 layers 為空 → throw with `err.code === 'BAD_CSV'`
    - 呼叫端 (`saveSheetUrl`, `refreshWorkOrder`) 捕到後依 code 叫對應 showNotify 文案
    - 斷線（fetch TypeError）也歸入 NOT_PUBLIC 類（user 無法區分；訊息覆蓋性足夠）
    - 快取 fallback 行為不變：fetchTodayWorkOrder 非 force 呼叫時若有當日 1hr 內快取仍直接回傳
  </behavior>
  <action>
    1. 在 fetchTodayWorkOrder 中改造 throw：
       ```js
       const csvUrl = sheetUrlToCsv(url);
       if (!csvUrl) { const e = new Error('URL 格式無效'); e.code = 'BAD_URL'; throw e; }
       let resp;
       try { resp = await fetch(csvUrl); }
       catch (netErr) { const e = new Error('網路或權限錯誤'); e.code = 'NOT_PUBLIC'; throw e; }
       if (!resp.ok) { const e = new Error(`HTTP ${resp.status}`); e.code = 'NOT_PUBLIC'; throw e; }
       const csv = await resp.text();
       let layers;
       try { layers = parseWorkOrderCsv(csv); }
       catch (parseErr) { const e = new Error('CSV 格式錯誤'); e.code = 'BAD_CSV'; throw e; }
       if (!layers.length) { const e = new Error('CSV 無有效列'); e.code = 'BAD_CSV'; throw e; }
       ```

    2. 新增 helper（放 showNotify 附近）：
       ```js
       function notifyWorkOrderError(err) {
         const MSG = {
           BAD_URL: '工單 URL 格式錯誤，請確認是 Google Sheet 連結',
           NOT_PUBLIC: '無法讀取 Sheet，請確認已設為「知道連結的任何人可檢視」',
           BAD_CSV: 'Sheet 欄位格式錯誤，請確認第一列為標題、從第二列開始填'
         };
         showNotify(MSG[err.code] || ('拉取失敗：' + err.message), 'error');
       }
       ```

    3. 修改 saveSheetUrl catch：
       - 保留 sheetStatus 紅字顯示（現行行為）
       - 新增 `notifyWorkOrderError(err)`
       - 若 err.code === 'BAD_URL'，先前的早期檢查 `if (!sheetUrlToCsv(url))` 已擋，但 fetchTodayWorkOrder 仍可能再拋；兩邊訊息一致即可

    4. 修改 refreshWorkOrder catch：同樣在 catch 內加 `notifyWorkOrderError(err)`

    5. 早期 `if (!sheetUrlToCsv(url)) { showNotify('URL 格式錯誤', 'warn'); return; }`（第 811 行）改為使用 BAD_URL 訊息：
       `showNotify('工單 URL 格式錯誤，請確認是 Google Sheet 連結', 'error');`
  </action>
  <verify>
    <automated>grep -nE "BAD_URL|NOT_PUBLIC|BAD_CSV" app.js | wc -l</automated>
    <automated>grep -n "工單 URL 格式錯誤\|無法讀取 Sheet\|Sheet 欄位格式錯誤" app.js  # 三訊息都在</automated>
    手動：
    1. 在 sheet URL 輸入框貼「https://example.com」→ 拉取 → 橫幅顯示 BAD_URL 訊息
    2. 綁一個私有的 Sheet URL（未公開）→ 拉取 → 橫幅顯示 NOT_PUBLIC 訊息
    3. 綁一個公開但格式錯的 Sheet（例如空表或欄位順序錯）→ 拉取 → 橫幅顯示 BAD_CSV 訊息
  </verify>
  <done>三種錯誤皆對應正確 showNotify 訊息；既有 fallback 本地快取行為不變。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: refreshWorkOrder 加入新舊工單 diff + showConfirm 覆蓋流程</name>
  <files>app.js</files>
  <behavior>
    - 按「🔄 重新拉工單」→ 強制 force=true fetch 新工單
    - 若 `Storage.getWorkOrderRaw()` 無資料或非當日 → 不比 diff，直接寫入 + 推送 saveWorkOrder（首次拉路徑）
    - 若有當日本地快取：
      - 比對新/舊 layers，產出 diff：新增（only in new）、刪除（only in old）、變更（同 batch key 但 product/flavor 有異）
      - diff 完全為空 → `showNotify('工單無變更','ok')`，不覆蓋、不推送
      - diff 非空 → 組 HTML 清單 → `await showConfirm('重新拉工單','<diff>','覆蓋','取消')`
      - 使用者按覆蓋 → `Storage.setWorkOrder(newWo)` + 推送 saveWorkOrder + `showNotify('✅ 工單已更新','success')` + `renderWorkOrderPreview(newLayers)`
      - 使用者按取消 → 不動快取、不推送、不顯示額外訊息
    - fetchTodayWorkOrder 失敗：照 Task 1 的錯誤分類 showNotify，不進 diff 流程
  </behavior>
  <action>
    1. 在 app.js 合適位置（refreshWorkOrder 附近）新增 helper：
       ```js
       function diffWorkOrder(oldLayers, newLayers) {
         const oldMap = new Map((oldLayers||[]).map(l => [l.batch, l]));
         const newMap = new Map((newLayers||[]).map(l => [l.batch, l]));
         const added = [], removed = [], changed = [];
         for (const [k, nl] of newMap) {
           const ol = oldMap.get(k);
           if (!ol) added.push(nl);
           else if (ol.product !== nl.product || ol.flavor !== nl.flavor) changed.push({ old: ol, new: nl });
         }
         for (const [k, ol] of oldMap) {
           if (!newMap.has(k)) removed.push(ol);
         }
         return { added, removed, changed, empty: !added.length && !removed.length && !changed.length };
       }

       function formatDiffHtml(diff) {
         const rows = [];
         diff.added.forEach(l => rows.push(`<div style="color:#27AE60">➕ ${l.batch} ${l.product}｜${l.flavor}</div>`));
         diff.removed.forEach(l => rows.push(`<div style="color:#E74C3C">➖ ${l.batch} ${l.product}｜${l.flavor}</div>`));
         diff.changed.forEach(c => rows.push(`<div style="color:#E67E22">♻ ${c.new.batch} ${c.old.product}｜${c.old.flavor} → ${c.new.product}｜${c.new.flavor}</div>`));
         return `<div style="max-height:300px;overflow:auto;font-size:14px;line-height:1.8">${rows.join('')}</div>`;
       }
       ```

    2. 改寫 refreshWorkOrder：
       - 改為不再直接呼叫 fetchTodayWorkOrder（因 fetchTodayWorkOrder 會直接寫 Storage）；改為**不寫入**的純拉取版本，或先拉回 layers、稍後由 diff 邏輯決定是否寫。建議做法：新增 internal helper `async function fetchWorkOrderRaw()`，做完 URL/fetch/parse 後只 return `{ layers, wo }`，不呼叫 Storage.setWorkOrder。fetchTodayWorkOrder 可以依舊內部使用 fetchWorkOrderRaw 再 setWorkOrder；refreshWorkOrder 則只取 raw、跑 diff、由使用者決定是否 setWorkOrder。
       - 失敗 → `notifyWorkOrderError(err)` 後 return
       - 成功後：
         ```js
         const oldWo = Storage.getWorkOrderRaw();
         if (!oldWo || oldWo.date !== dateStr || !oldWo.layers?.length) {
           Storage.setWorkOrder(newWo); pushWorkOrderToCloud(newWo);  // 首次
           renderWorkOrderPreview(newWo.layers);
           showNotify(`✅ 工單已拉取（${newWo.layers.length} 層）`,'success');
           return;
         }
         const diff = diffWorkOrder(oldWo.layers, newWo.layers);
         if (diff.empty) { showNotify('工單無變更','ok'); return; }
         const ok = await showConfirm('重新拉工單', formatDiffHtml(diff), '覆蓋', '取消');
         if (!ok) return;
         Storage.setWorkOrder(newWo);
         pushWorkOrderToCloud(newWo);  // grep 既有 saveWorkOrder 推送函式名，替換
         renderWorkOrderPreview(newWo.layers);
         showNotify('✅ 工單已更新','success');
         ```

    3. **重要**：grep 確認 e3bb1e3 加入的 saveWorkOrder 推送函式確切名字（可能是 `syncWorkOrderToCloud`、`pushWorkOrder`、或直接 inline fetch）。用實際存在的函式名，不要重新實作推送邏輯。

    4. 既有「🔄 重新拉工單」按鈕的 handler 對應到 refreshWorkOrder 即可（現已綁定，無需動 HTML）。
  </action>
  <verify>
    <automated>grep -n "diffWorkOrder\|formatDiffHtml" app.js  # 新 helper 存在</automated>
    <automated>grep -n "showConfirm.*覆蓋\|showConfirm.*重新拉" app.js  # diff confirm 流程接通</automated>
    <automated>grep -n "工單無變更" app.js  # 相同情境訊息</automated>
    手動（checkpoint 會驗）：
    1. 首次綁 Sheet 拉取 → 無 diff modal、直接寫入並推雲
    2. Sheet 不動，再按「🔄 重新拉工單」→ showNotify「工單無變更」
    3. 改 Sheet 一列口味 → 重新拉 → 跳 showConfirm 顯示 ♻ 變更列 → 取消 → 本地仍為舊版
    4. 再按一次 → 覆蓋 → 本地快取更新、Sheets「工單」分頁出現新版（saveWorkOrder 推送成功）
  </verify>
  <done>diff 流程完整、首次/無變更/有變更三分支正確；saveWorkOrder 推送仍在覆蓋路徑上。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - fetchTodayWorkOrder 三類錯誤（BAD_URL / NOT_PUBLIC / BAD_CSV）
    - 對應 showNotify 橫幅訊息
    - refreshWorkOrder 加入新舊 diff + showConfirm 覆蓋流程
    - 首次拉 / 無變更 / 有變更三分支
  </what-built>
  <how-to-verify>
    1. 錯誤訊息三類測試（Task 1 verify 第 1-3 項）
    2. Diff 流程：
       a. 準備兩個版本的 Google Sheet 工單（或同 Sheet 改一筆 flavor）
       b. 首次綁定（本地無 bakery_workorder）→ 拉取 → 不應跳 diff modal
       c. 不改 Sheet，按「🔄 重新拉工單」→ 橫幅顯示「工單無變更」
       d. Sheet 修改一列（例如 1-1 口味從「天使」→「原味」，或新增 1-3）→ 重新拉 → 跳 showConfirm 列出差異
       e. 按取消 → 本地 bakery_workorder 不變、Sheets 工單分頁不變
       f. 再按一次重新拉 → 按覆蓋 → 本地更新、Sheets 工單分頁出現新版
    3. 斷網情境：
       a. DevTools Network → Offline → 重新拉 → 橫幅顯示 NOT_PUBLIC 訊息
       b. 既有本地快取仍可用（工單預覽仍顯示舊資料）
  </how-to-verify>
  <resume-signal>回覆「approved」或描述問題</resume-signal>
</task>

</tasks>

<verification>
- 三類錯誤訊息繁中正確
- Diff 流程三分支（首次/無變更/有變更）各自正確
- 覆蓋時 Sheets「工單」分頁同步更新（saveWorkOrder 推送）
- 拉取失敗 fallback 本地快取行為未被破壞
</verification>

<success_criteria>
- 達成 Phase 2 Success Criteria #2（換 gid 重整 → 當日批次改變；斷網用快取）
- 達成 Phase 2 Success Criteria #3（工單存本地同時推到 Sheets）
- 達成 Phase 2 Success Criteria #4（三種錯誤情境都有對應提示）
- 通過 checkpoint 所有手動驗證步驟
</success_criteria>

<output>
完成後建立 `.planning/phases/02-multi-layer-ux-sheet-fetch/02-02-SUMMARY.md`
</output>
