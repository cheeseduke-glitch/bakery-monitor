---
phase: 02-multi-layer-ux-sheet-fetch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - PRD_起士公爵生產良率監測系統.md
autonomous: true
requirements: [R3.3]
must_haves:
  truths:
    - "PRD §10.2「手動同步」字樣已改為「自動同步 + bakery_sync_queue 佇列」描述"
    - "PRD §8 分析頁資料來源說明明寫「優先雲端 (pullFromCloud)，fallback 本地 localStorage」"
    - "PRD §12「單 HTML < 50KB」已改為「初次載入 < 100KB（index.html + styles.css + app.js gzip 後）」"
  artifacts:
    - path: "PRD_起士公爵生產良率監測系統.md"
      provides: "§10.2 / §8 / §12 三處文案校正"
      contains: "自動同步|bakery_sync_queue|pullFromCloud|初次載入 < 100KB"
  key_links: []
---

<objective>
落實 D4：同步校正 PRD 三處文案，消除 v2.1 設計與文件落差。純文件編輯，與 02-01/02-02 不共享任何程式區域，可與 02-01 並行執行（Wave 1）。

Purpose: PRD 必須與 SDD v1.1 與實際程式一致；避免後續工程師／審閱者看到矛盾描述。
Output: 校正後的 `PRD_起士公爵生產良率監測系統.md`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-layer-ux-sheet-fetch/02-CONTEXT.md
@SDD.md
@PRD_起士公爵生產良率監測系統.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 定位並校正 PRD §10.2 / §8 / §12 三處文案</name>
  <files>PRD_起士公爵生產良率監測系統.md</files>
  <action>
    1. 開啟 `PRD_起士公爵生產良率監測系統.md`，用 Grep / Read 定位三個章節：
       - §10.2（離線 / 同步相關段落）
       - §8（分析頁描述段落）
       - §12（NFR / 效能 / 檔案大小段落）

    2. **§10.2 文案**：
       - 原文包含：「離線存 localStorage，上線後**手動**同步」（或類似「手動同步」「手動觸發」字樣）
       - 改為：「離線存 localStorage，上線後**自動**同步（記錄進 `bakery_sync_queue` 佇列，由 `flushQueue` 於網路恢復時重送）」
       - 若該段還有提到手動觸發同步的 UI 描述，一併校正；但不要加入尚未實作的 UI 規格，僅描述機制。
       - 註記：`bakery_sync_queue` 是 Phase 3 才實作的東西，但 PRD 作為目標文件應描述最終狀態。若文中強烈暗示 v2.1 已上線，可加一句「v2.1 已完成；佇列機制於 Phase 3 補實作」。

    3. **§8 文案**：
       - 在分析頁描述處補上或修訂一行：「分析頁資料來源：優先雲端 (`pullFromCloud`)，fallback 本地 localStorage」
       - 若該段已有類似描述但不完整，擴充為上述完整句；不要重複寫兩次。

    4. **§12 文案**：
       - 原文：「單 HTML < 50KB」（或任何明寫 50KB 的效能目標）
       - 改為：「初次載入 < 100KB（index.html + styles.css + app.js gzip 後）」
       - 若 §12 還有其他 NFR 條列與此衝突（例如「單檔 app」）一併調整為多檔描述。

    5. 不得改動其他章節，不得調整標題編號。
  </action>
  <verify>
    <automated>grep -n "手動同步\|單 HTML < 50KB\|&lt;50KB" "PRD_起士公爵生產良率監測系統.md"  # 應為 0 行（都被改掉）</automated>
    <automated>grep -n "自動同步\|bakery_sync_queue\|flushQueue" "PRD_起士公爵生產良率監測系統.md"  # 應 >= 1 行</automated>
    <automated>grep -n "pullFromCloud\|優先雲端" "PRD_起士公爵生產良率監測系統.md"  # §8 更新後應 >= 1 行</automated>
    <automated>grep -n "初次載入 < 100KB\|100KB" "PRD_起士公爵生產良率監測系統.md"  # §12 更新後應 >= 1 行</automated>
  </verify>
  <done>三處文案已校正；git diff 只顯示 PRD 檔案變動，無副作用。</done>
</task>

</tasks>

<verification>
- 三個 grep 正向檢查全中
- 一個 grep 反向檢查（舊字樣）全清
- git diff --stat 只列出 PRD 檔案
</verification>

<success_criteria>
- PRD §10.2 / §8 / §12 三處文案與 SDD v1.1 + 實際程式一致
- 無副作用變更（其他章節、其他檔案皆未動）
</success_criteria>

<output>
完成後建立 `.planning/phases/02-multi-layer-ux-sheet-fetch/02-03-SUMMARY.md`
</output>
