---
phase: 02-multi-layer-ux-sheet-fetch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - styles.css
  - app.js
autonomous: false
requirements: [R1.3]
must_haves:
  truths:
    - "均質/蛋白霜站勾 2 層以上送出時會跳確認視窗，列出所有將寫入的層"
    - "使用者按取消後，勾選狀態保留、不寫入任何 Record"
    - "使用者按確認後，selectedLayers 全部進入既有 submitStation 寫入迴圈，localStorage 出現 N 筆 Record"
    - "勾 1 層時直接送出，不彈任何確認窗（保留現行行為）"
    - "showConfirm API 可被後續 plan（02-02 的重新拉工單差異）重用"
  artifacts:
    - path: "index.html"
      provides: "新增 #confirmModal DOM（標題 / 內容 / 確認 / 取消）"
      contains: "confirmModal"
    - path: "styles.css"
      provides: "confirm modal 樣式（沿用 .modal-overlay / .modal 視覺，雙按鈕排列）"
    - path: "app.js"
      provides: "showConfirm(title, bodyHtml, okLabel, cancelLabel) → Promise<boolean>；submitStation 前置 guard"
      exports: ["showConfirm"]
  key_links:
    - from: "app.js submitStation()"
      to: "showConfirm()"
      via: "若 AppState.selectedLayers.length >= 2 → await showConfirm(...) → false 則 return"
      pattern: "selectedLayers\\.length\\s*>=\\s*2"
---

<objective>
落實 D1：均質/蛋白霜站勾 2 層以上送出時先彈確認視窗，列出將寫入的層清單，使用者按確認才進入既有 submitStation 寫入迴圈。同時交付可重用的 showConfirm(title, bodyHtml, okLabel, cancelLabel) modal API，供 02-02 的「重新拉工單差異確認」沿用。

Purpose: 避免誤勾多層一口氣寫入 3 筆錯誤記錄；建立可重用的確認 modal 元件。
Output: index.html 新增 #confirmModal、styles.css 補樣式、app.js 新增 showConfirm 與 submitStation 前置 guard。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-layer-ux-sheet-fetch/02-CONTEXT.md
@SDD.md
@index.html
@styles.css
@app.js

<interfaces>
現有程式關鍵介面（必讀，不要重新發明）：

index.html 既有 modal 結構（第 199-205 行）—— #confirmModal 應沿用相同骨架：
```html
<div class="modal-overlay" id="resultModal">
  <div class="modal">
    <h2 id="modalTitle"></h2>
    <div id="modalContent"></div>
    <button class="btn btn-primary" data-action="closeModal">確認</button>
  </div>
</div>
```

app.js 既有 helper：
- `showNotify(text, type)`（第 902 行）—— level: 'ok'|'warn'|'error'|'critical'|'success'
- `AppState.selectedLayers`（陣列，元素為 batchId 字串如 "1-1"）
- `submitStation()`（第 363 行）—— 第一個動作為 selectedLayers 非空檢查，第二個為 operator 檢查

data-action dispatcher：既有按鈕透過 `data-action="closeModal"` 綁定；新按鈕請沿用此 pattern 或直接 addEventListener。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 新增 #confirmModal DOM + 樣式</name>
  <files>index.html, styles.css</files>
  <behavior>
    - #confirmModal 預設隱藏（無 .show class）
    - 呼叫 showConfirm 時加 .show，按任一鈕後移除
    - 雙按鈕並排（取消在左 / 確認在右），手機觸控目標 ≥ 44px
  </behavior>
  <action>
    在 index.html 第 205 行後、第 206 行 #notifyBanner 前插入 #confirmModal：
    ```html
    <div class="modal-overlay" id="confirmModal">
      <div class="modal">
        <h2 id="confirmTitle"></h2>
        <div id="confirmContent"></div>
        <div class="confirm-actions">
          <button class="btn btn-secondary" id="confirmCancelBtn">取消</button>
          <button class="btn btn-primary" id="confirmOkBtn">確認</button>
        </div>
      </div>
    </div>
    ```

    在 styles.css 末尾新增 `.confirm-actions` 樣式：flex 排列、gap 12px、margin-top 16px；兩個 button 各佔 flex:1；min-height 44px。若專案尚無 `.btn-secondary` class，新增灰底樣式（背景 #E5E7EB、文字 #374151）。

    重要：不得修改既有 #resultModal 任何結構（D1 指定 modal 行為不同，另建獨立元件，per 02-CONTEXT Specifics）。
  </action>
  <verify>
    <automated>grep -n "confirmModal" index.html styles.css 2>/dev/null | wc -l  # 預期 >= 2</automated>
    手動：瀏覽器 DevTools Elements 找得到 #confirmModal，預設不可見（無 .show class 時 display:none）。
  </verify>
  <done>#confirmModal DOM 就位；.confirm-actions 樣式在 styles.css；#resultModal 未被改動。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 實作 showConfirm API + submitStation 前置 guard</name>
  <files>app.js</files>
  <behavior>
    - showConfirm(title, bodyHtml, okLabel='確認', cancelLabel='取消') 回傳 Promise<boolean>
    - 按確認 resolve(true)；按取消或點 overlay 外部 resolve(false)
    - 同時只會有一個 confirm modal（若重複呼叫，前一個先被 reject 或自動取消）
    - submitStation：若 AppState.selectedLayers.length >= 2，在既有 operator 檢查通過後、進入資料收集前呼叫 showConfirm
    - showConfirm 回傳 false → submitStation 直接 return（不清除勾選、不寫入）
    - selectedLayers.length === 1 → 完全跳過 showConfirm（現行行為保留）
  </behavior>
  <action>
    1. 在 `showNotify` 附近（第 902 行左右）新增 showConfirm helper：
       - 取得 #confirmModal / #confirmTitle / #confirmContent / #confirmOkBtn / #confirmCancelBtn
       - 設定 title / bodyHtml（bodyHtml 用 innerHTML，呼叫端負責 escape；Phase 4 會統一處理 XSS，現階段呼叫端只傳固定字串/已 escape 內容）
       - 設定按鈕文字
       - 回傳 new Promise：綁定一次性 click handler，resolve 後移除 .show、移除 handler
       - 確認鈕 resolve(true)、取消鈕 resolve(false)

    2. 修改 submitStation（第 363 行起）：
       - 既有的 selectedLayers 空檢查與 operator 檢查保留不動
       - 在 operator 檢查通過後、進入 `const fields=STATION_FIELDS[...]` 之前插入：
         ```js
         if (AppState.selectedLayers.length >= 2) {
           const list = AppState.selectedLayers.join('、');
           const body = `<p>將寫入 <b>${AppState.selectedLayers.length}</b> 筆記錄：</p><p style="font-size:14px;color:#666">${list}</p>`;
           const ok = await showConfirm(`確認寫入 ${AppState.station}站`, body, '確認送出', '取消');
           if (!ok) return;
         }
         ```
       - 將 `function submitStation()` 改為 `async function submitStation()`
       - 檢查 submitStation 所有呼叫點（data-action dispatcher 或 onclick）—— 若沒處理回傳值，async 化不影響邏輯，可忽略

    3. 取消後狀態：selectedLayers 不動、勾選 UI 不動（CONTEXT D1：「取消後停留原頁面，勾選狀態不清除」）
  </action>
  <verify>
    <automated>grep -n "async function submitStation\|function showConfirm" app.js  # 兩者皆出現</automated>
    <automated>node -e "const c=require('fs').readFileSync('app.js','utf8');if(!c.includes('selectedLayers.length >= 2'))process.exit(1);if(!c.match(/await\s+showConfirm/))process.exit(1);console.log('ok')"</automated>
    手動（checkpoint 會驗）：
    1. 均質站勾 1-1, 1-2, 1-3 → 送出 → 跳 confirm modal 列出 3 層
    2. 按取消 → 視窗關、勾選仍在、localStorage 無新記錄
    3. 按確認 → localStorage 出現 3 筆 Record（batch 分別為 1-1, 1-2, 1-3）
    4. 只勾 1-1 → 送出 → 不跳 confirm，直接進結果 modal
  </verify>
  <done>showConfirm 可被外部呼叫並回傳 Promise<boolean>；submitStation 勾 2 層以上會攔截；勾 1 層行為不變。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - 新增 #confirmModal（index.html / styles.css）
    - showConfirm(title, bodyHtml, okLabel, cancelLabel) helper
    - submitStation 勾 2 層以上先彈確認窗
  </what-built>
  <how-to-verify>
    1. 開啟 index.html（本地 server 或 file://）
    2. 至均質站選一個操作人員 → 勾 1-1、1-2、1-3 三層 → 填完必填項 → 按「送出」
       - 預期：跳出確認窗「將寫入 3 筆記錄：1-1、1-2、1-3」
    3. 按取消 → 視窗關、勾選狀態仍在、DevTools Application → Local Storage → bakery_records 無新增
    4. 再按送出 → 跳同樣確認窗 → 按確認送出 → localStorage 出現 3 筆同 station、batch 為 1-1/1-2/1-3 的 Record
    5. 回均質站只勾 1-2 → 送出 → 直接進結果 modal，不跳 confirm
    6. 整面站（單選）勾 1-1 → 送出 → 直接進結果 modal，不跳 confirm
  </how-to-verify>
  <resume-signal>回覆「approved」或描述問題</resume-signal>
</task>

</tasks>

<verification>
- 勾 2+ 層 → confirm modal 出現、列出正確層清單
- 取消不寫入、不清勾選
- 確認後寫入 N 筆 Record
- 單選 / 非多層站維持現行行為
- showConfirm 可重用（02-02 會用到）
</verification>

<success_criteria>
- index.html 含 #confirmModal，styles.css 含 .confirm-actions
- app.js 含 `async function submitStation` 與 `function showConfirm`
- 手動驗證通過 checkpoint 所有步驟
- 達成 Phase 2 Success Criteria #1（均質站一次勾 3 層 → 3 筆 Record）
</success_criteria>

<output>
完成後建立 `.planning/phases/02-multi-layer-ux-sheet-fetch/02-01-SUMMARY.md`
</output>
